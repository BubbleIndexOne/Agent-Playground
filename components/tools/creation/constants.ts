/**
 * @fileoverview Constants for Tool Creation UI
 *
 * Defines the available primitive and composite parameter types supported in the
 * tool parameter builder dropdown.
 */

import { ParameterType } from '../types'

/**
 * List of available parameter data types selectable in the parameter builder.
 */
export const PARAM_TYPES: ParameterType[] = ['string', 'number', 'boolean', 'object', 'array']

